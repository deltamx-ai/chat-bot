{
  "name": "chat-bot-workspace",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "build:web": "npm --prefix apps/web run build",
    "check:web": "npm --prefix apps/web run build"
  }
}
