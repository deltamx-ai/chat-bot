# desktop

Tauri 桌面端项目。

## 结构

- `src/`：React + TypeScript + Vite 前端
- `src-tauri/`：Tauri Rust 壳

## 常用命令

```bash
npm install
npm run dev
npm run lint
npm run build
cargo check --manifest-path src-tauri/Cargo.toml
```
